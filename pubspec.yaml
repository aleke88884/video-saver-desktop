name: video_desktop_saver
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.2
  supabase_flutter: ^2.8.4
  cloudinary_flutter: ^1.3.0
  cloudinary_url_gen: ^1.7.0
  provider: ^6.1.4
  go_router: ^14.2.0
  flutter_screenutil: ^5.9.0
  window_manager: ^0.4.0
  file_picker: ^10.1.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/images/
    # - assets/vectors/

  uses-material-design: true
